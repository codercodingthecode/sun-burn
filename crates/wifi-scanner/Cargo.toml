[package]
name = "wifi-scanner"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[target.'cfg(target_os = "macos")'.dependencies]
wifi_scan = "0.7"
